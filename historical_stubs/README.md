# Historical Non-Production Stubs

This directory contains legacy, experimental, or public overview stub code that is not synchronized with the private DroneOS-Lab repository.

The code here is for historical reference only and must not be used for real flight validation, hardware deployment, or operational decision-making.

Moved directories:
- `drone_api/`
- `drone_core/`
- `droneos-collab-path/`
- `tasks/`
